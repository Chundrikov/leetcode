package Practice;

/**
 * Ближайший ноль
 */
public class NearestZero {

    /*
    public int[] findNearestZeroWithMap(int streetLength, int[] sequence) {
        // Map solution

        int [] result = new int[streetLength];

        Map<Integer, Integer> mapFromArray = new HashMap<>();
        for (int i = 0; i < sequence.length; i++) {
            mapFromArray.put(i, sequence[i]);
        }

        mapFromArray.forEach();


        return result;
    }

     */
}
